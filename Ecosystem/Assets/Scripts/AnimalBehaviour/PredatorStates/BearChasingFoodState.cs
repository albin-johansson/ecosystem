using UnityEngine;

namespace Ecosystem.AnimalBehaviour.PredatorStates
{
  public class BearChasingFoodState : MonoBehaviour
  {
    // Start is called before the first frame update
    void Start() { }

    // Update is called once per frame
    void Update() { }
  }
}