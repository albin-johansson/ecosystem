using UnityEngine;

namespace Ecosystem.AnimalBehaviour.PreyStates
{
  public class DeerLookingForMateState : MonoBehaviour
  {
    // Start is called before the first frame update
    void Start() { }

    // Update is called once per frame
    void Update() { }
  }
}